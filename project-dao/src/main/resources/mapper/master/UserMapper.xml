<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spring.boot.dao.web.master.UserDao">
	<resultMap id="User" type="com.spring.boot.bean.master.User">
		<result column="id" property="userId" />
		<result column="userName" property="userName" />
		<result column="password" property="password" />
		<result column="account" property="account" />
	</resultMap>

	<!--<parameterMap id="user" type="com.spring.boot.bean.User"/>-->

	<sql id="Base_Column_List">
		id, userName, password, account
	</sql>

	<select id="findByUserId" resultMap="User" parameterType="map">
		select
		<include refid="Base_Column_List" />
		from user
		where id = #{userId}
	</select>

	<select id="findByUserAccount" resultMap="User" parameterType="map">
		select
		<include refid="Base_Column_List" />
		from user
		where account = #{account}
	</select>

</mapper>
